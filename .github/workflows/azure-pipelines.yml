name: Terraform + .NET ZIP Deploy

on:
  push:
    branches: [ main ]

permissions:
  id-token: write
  contents: read

env:
  TF_WORKING_DIR: ./terraform
  DOTNET_VERSION: "8.0.x"
  PROJECT_PATH: terraform/appServiceSlot/SlotDemo
  # PROJECT_PATH1: terraform/appServiceSlot/appServiceSlot/SlotDemo
  PROJECT_FILE: SlotDemo.csproj
  
jobs:
  terraform-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # - name: Disable GitHub Action command wrapping for Terraform ✅
      #   run: echo "DISABLE_GITHUB_COMMAND_WRAPPING=true" >> $GITHUB_ENV
      # - name: Disable Terraform Wrapper ✅
      #   shell: bash
      #   run: |
      #    echo "DISABLE_GITHUB_COMMAND_WRAPPING=true" >> $GITHUB_ENV
      #    echo "GITHUB_IS_ACTION=false" >> $GITHUB_ENV
      #    export GITHUB_IS_ACTION=false
      #    export DISABLE_GITHUB_COMMAND_WRAPPING=true

      #    echo "✅ Terraform wrapper disabled"



      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: "1.8.5"

     


      - name: Azure Login
        uses: azure/login@v2
        with:
           client-id: ${{ secrets.AZURE_CLIENT_ID }}
           client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
           auth-type: service_principal
      # - name: Disable GitHub Terraform Wrapper ✅
      #   run: |
      #    echo "DISABLE_GITHUB_COMMAND_WRAPPING=true" >> $GITHUB_ENV
      #    sudo ln -sf $(which terraform) /usr/local/bin/terraform
      #    terraform -version


      - name: Terraform Init
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform init

      # - name: Terraform Plan
      #   working-directory: ${{ env.TF_WORKING_DIR }}
      #   run: terraform plan -out=tfplan
      


      - name: Terraform Apply
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: terraform apply -auto-approve

      # - name: Save Terraform Outputs to File ✅
      #   working-directory: ${{ env.TF_WORKING_DIR }}
      #   run: |
      #    terraform output -json > tf_outputs.json
      #    ls -l
      #    cat tf_outputs.json

      # - name: ✅ Clean Terraform environment
      #   run: |
      #    unset GITHUB_ENV
      #    export TF_IN_AUTOMATION=false
      #    export TF_LOG=OFF
      #    export CHECKPOINT_DISABLE=1

      - name: Extract Terraform Outputs (Clean)
        working-directory: ${{ env.TF_WORKING_DIR }}
        run: |
         echo "🔍 Checking Terraform output status..."
         terraform output || true

         # Capture output including wrapper noise
         RAW_WEBAPP_NAME=$(terraform output -raw webapp_name 2>&1)
         RAW_WEBAPP_SLOT=$(terraform output -raw slot_name 2>&1)
         RAW_WEBAPP_RG=$(terraform output -raw resource_group_name 2>&1)

         # ✅ Strip GitHub Action wrapper paths and debug string
         WEBAPP_NAME=$(echo "$RAW_WEBAPP_NAME" | sed -E 's/^\[command\].*output -raw webapp_name//; s/::debug::.*//; s/^ *//')
         WEBAPP_SLOT=$(echo "$RAW_WEBAPP_SLOT" | sed -E 's/^\[command\].*output -raw slot_name//; s/::debug::.*//; s/^ *//')
         WEBAPP_RG=$(echo "$RAW_WEBAPP_RG" | sed -E 's/^\[command\].*output -raw resource_group_name//; s/::debug::.*//; s/^ *//')

         echo "WEBAPP_NAME=$WEBAPP_NAME" >> $GITHUB_ENV
         echo "WEBAPP_SLOT=$WEBAPP_SLOT" >> $GITHUB_ENV
         echo "WEBAPP_RG=$WEBAPP_RG" >> $GITHUB_ENV

         echo "Final Clean Output:"
         echo "App-name: $WEBAPP_NAME"
         echo "slot-name: $WEBAPP_SLOT"
         echo "resource-group-name: $WEBAPP_RG"









        #  JSON=$(terraform output -json)

        #  echo "WEBAPP_NAME=$(echo $JSON | jq -r '.webapp_name.value')" >> $GITHUB_ENV
        #  echo "WEBAPP_SLOT=$(echo $JSON | jq -r '.slot_name.value')" >> $GITHUB_ENV
        #  echo "WEBAPP_RG=$(echo $JSON | jq -r '.resource_group_name.value')" >> $GITHUB_ENV

        # echo "✅ WebApp Name: $WEBAPP_NAME"
        # echo "✅ Slot Name: $WEBAPP_SLOT"
        # echo "✅ Resource Group: $WEBAPP_RG"


      
      # - name: Debug exported variables
      #   run: |
      #     echo "WEBAPP_NAME: $WEBAPP_NAME"
      #     echo "WEBAPP_SLOT: $WEBAPP_SLOT"
      #     echo "WEBAPP_RG: $WEBAPP_RG"

      # - name: ✅ Extract Terraform Outputs (Clean)
      #   working-directory: ${{ env.TF_WORKING_DIR }}
      #   run: |
      #     echo "Extracting terraform outputs..."

      #     WEBAPP_NAME=$(terraform output -raw webapp_name 2>/dev/null | xargs)
      #     WEBAPP_SLOT=$(terraform output -raw slot_name 2>/dev/null | xargs)
      #     WEBAPP_RG=$(terraform output -raw resource_group_name 2>/dev/null | xargs)

      #     echo "WEBAPP_NAME=$WEBAPP_NAME" >> $GITHUB_ENV
      #     echo "WEBAPP_SLOT=$WEBAPP_SLOT" >> $GITHUB_ENV
      #     echo "WEBAPP_RG=$WEBAPP_RG" >> $GITHUB_ENV

      #     echo "✅ Clean Outputs:"
      #     echo "WEBAPP_NAME: $WEBAPP_NAME"
      #     echo "WEBAPP_SLOT: $WEBAPP_SLOT"
      #     echo "WEBAPP_RG: $WEBAPP_RG"

      # - name: Get Terraform Outputs
      #   id: tf-output
      #   working-directory: ${{ env.TF_WORKING_DIR }}
      #   run: |
      #    echo "Fetching Terraform outputs..."
    
      #    webapp_name=$(terraform output -raw webapp_name | tr -d '\r\n')
      #    slot_name=$(terraform output -raw slot_name | tr -d '\r\n')
      #    resource_group_name=$(terraform output -raw resource_group_name | tr -d '\r\n')

      #    echo "WEBAPP_NAME=$webapp_name" >> $GITHUB_ENV
      #    echo "WEBAPP_SLOT=$slot_name" >> $GITHUB_ENV
      #    echo "WEBAPP_RG=$resource_group_name" >> $GITHUB_ENV

      #    echo "WEBAPP_NAME: $webapp_name"
      #    echo "WEBAPP_SLOT: $slot_name"
      #    echo "WEBAPP_RG: $resource_group_name"

      # - name: Get Terraform Outputs
      #   id: tf-output
      #   working-directory: ${{ env.TF_WORKING_DIR }}
      #   run: |
      #     echo "WEBAPP_NAME=$(terraform output -raw webapp_name | tr -d '\r\n')" >> $GITHUB_ENV
      #     echo "WEBAPP_SLOT=$(terraform output -raw slot_name | tr -d '\r\n')" >> $GITHUB_ENV
      #     echo "WEBAPP_RG=$(terraform output -raw resource_group_name | tr -d '\r\n')" >> $GITHUB_ENV
      # - name: Check installed .NET Versions
      #   run: dotnet --list-sdks

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      # - name: Check folder structure
      #   run: |
      #    echo "Current directory:"
      #    pwd
      #    echo "Files:"
      #    ls -R

      - name: Restore & Build .NET App
        working-directory: ${{ env.PROJECT_PATH }}
        run: |
         PROJECT_FILE=$(find . -name "*.csproj" | head -n 1)
         echo "✅ Using project file: $PROJECT_FILE"
    
         dotnet restore "$PROJECT_FILE"
         dotnet publish "$PROJECT_FILE" -c Release -o ../../publish

      # - name: Create ZIP package
      #   working-directory: ${{ env.PROJECT_PATH }}
      #   run: |
      #    echo "📍 Current location: $(pwd)"
      #    ls -R bin/Release/net8.0/
      #    zip -r ../appServiceSlot.zip bin/Release/net8.0/publish
      #    ls -lh ../appServiceSlot.zip

      # - name: Create ZIP package
      #   working-directory: ${{ env.PROJECT_PATH }}
      #   run: |
      #    echo "📍 Current location: $(pwd)"
      #    ls -R
      #    zip -r appServiceSlot.zip publish

      # - name: Create ZIP package
      #   working-directory: ${{ env.PROJECT_PATH1 }}
      #   run: |
      #    cd publish
      #    zip -r ../appServiceSlot.zip
      #    cd ..


      # - name: Zip output
      #   run: |
      #     cd publish
      #     zip -r ../appServiceSlot.zip
      #     cd ..

      # - name: Deploy ZIP to Slot
      #   uses: azure/webapps-deploy@v3
      #   with:
      #     app-name: ${{ env.WEBAPP_NAME }}
      #     slot-name: ${{ env.WEBAPP_SLOT }}
      #     package: appServiceSlot.zip

      - name: Publish .NET App
        working-directory: ${{ env.PROJECT_PATH }}
        run: dotnet publish ${{ env.PROJECT_FILE }} -c Release -o bin/Release/net8.0/publish

      - name: Zip Published Output
        working-directory: ${{ env.PROJECT_PATH }}
        run: |
         cd bin/Release/net8.0
         zip -r appServiceSlot.zip publish
         mv appServiceSlot.zip ${{ github.workspace }}/
         

      - name: Deploy to Slot
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.WEBAPP_NAME }}
          slot-name: ${{ env.WEBAPP_SLOT }}
          resource-group-name: ${{ env.WEBAPP_RG }}
          package: ./appServiceSlot.zip

      # - name: Deploy to Azure App Service Slot
      #   uses: azure/webapps-deploy@v3
      #   with:
      #     app-name: ${{ env.WEBAPP_NAME }}
      #     slot-name: ${{ env.WEBAPP_SLOT }}
      #     package: ${{ github.workspace }}/appServiceSlot.zip

          # resource-group-name: ${{ env.WEBAPP_RG }}
          # package: appServiceSlot.zip



      - name: Smoke Test
        run: |
          curl -fS https://${{ env.WEBAPP_NAME }}-${{ env.WEBAPP_SLOT }}.azurewebsites.net || exit 1

      - name: Swap Slots
        run: |
          az webapp deployment slot swap \
          --resource-group ${{ env.WEBAPP_RG }} \
          --name ${{ env.WEBAPP_NAME }} \
          --slot ${{ env.WEBAPP_SLOT }} \
          --target-slot production


# name: Terraform + .NET ZIP Deploy

# on:
#   push:
#     branches: [ main ]

# permissions:
#   id-token: write
#   contents: read

# env:
#   TF_WORKING_DIR: ./terraform
#   DOTNET_VERSION: "8.0.x"
#   PROJECT_PATH: terraform/appServiceSlot/SlotDemo
#   PROJECT_FILE: SlotDemo.csproj

# jobs:
#   terraform-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v2
#         with:
#           terraform_version: "1.8.5"

#       - name: Azure Login
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           auth-type: service_principal

#       - name: Terraform Init
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         run: terraform init

#       - name: Terraform Apply
#         working-directory: ${{ env.TF_WORKING_DIR }}
#         run: terraform apply -auto-approve

#       - name: Extract Terraform Outputs
#         run: |
#           echo "Extracting terraform outputs..."
#           cd ${{ env.TF_WORKING_DIR }}

#           WEBAPP_NAME=$(terraform output -raw webapp_name)
#           WEBAPP_SLOT=$(terraform output -raw slot_name)
#           WEBAPP_RG=$(terraform output -raw resource_group_name)

#           echo "WEBAPP_NAME=$WEBAPP_NAME" >> $GITHUB_ENV
#           echo "WEBAPP_SLOT=$WEBAPP_SLOT" >> $GITHUB_ENV
#           echo "WEBAPP_RG=$WEBAPP_RG" >> $GITHUB_ENV

#     echo "WEBAPP_NAME: $WEBAPP_NAME"
#     echo "WEBAPP_SLOT: $WEBAPP_SLOT"
#     echo "WEBAPP_RG: $WEBAPP_RG"


#       - name: Setup .NET
#         uses: actions/setup-dotnet@v4
#         with:
#           dotnet-version: ${{ env.DOTNET_VERSION }}

#       - name: Restore & Publish App
#         working-directory: ${{ env.PROJECT_PATH }}
#         run: dotnet publish ${{ env.PROJECT_FILE }} -c Release -o publish

#       - name: Zip Artifact
#         working-directory: ${{ env.PROJECT_PATH }}
#         run: |
#           zip -r appServiceSlot.zip publish
#           mv appServiceSlot.zip ${{ github.workspace }}/

#       - name: Print Deployment Variables
#         run: |
#           echo "WEBAPP_NAME=${{ env.WEBAPP_NAME }}"
#           echo "WEBAPP_SLOT=${{ env.WEBAPP_SLOT }}"
#           echo "WEBAPP_RG=${{ env.WEBAPP_RG }}"

#       - name: Deploy to Azure App Service Slot
#         uses: azure/webapps-deploy@v3
#         with:
#           app-name: ${{ env.WEBAPP_NAME }}
#           slot-name: ${{ env.WEBAPP_SLOT }}
#           resource-group-name: ${{ env.WEBAPP_RG }}
#           package: appServiceSlot.zip

#       - name: Smoke Test
#         run: |
#           curl -fS https://${{ env.WEBAPP_NAME }}-${{ env.WEBAPP_SLOT }}.azurewebsites.net || exit 1

#       - name: Swap Slots (Staging → Production)
#         run: |
#           az webapp deployment slot swap \
#           --resource-group ${{ env.WEBAPP_RG }} \
#           --name ${{ env.WEBAPP_NAME }} \
#           --slot ${{ env.WEBAPP_SLOT }} \
#           --target-slot production